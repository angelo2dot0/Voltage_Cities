<%= form_for @user, :html =>{ :multipart => true } do |f| %>

    <%= f.text_field :artist_name %>
    <%= f.text_area :current_city %>
    <%= f.file_field :avatar %>
    <%= f.submit "Edit information" %>

<% end %>